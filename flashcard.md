How should the number of filters scale across layers in CNNs?
---
Filters should increase as depth increases (e.g., 32 → 64 → 128 → 256) while spatial dimensions decrease, since deeper layers need more capacity to capture complex high-level features.
